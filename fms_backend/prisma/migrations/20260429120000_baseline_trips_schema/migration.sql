-- Baseline migration to reflect current database schema
-- This migration marks all previous migrations as applied to match the current DB state

-- Trips table already has driverId and vehicleId foreign keys